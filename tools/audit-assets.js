const fs=require("fs"),path=require("path");const root=path.join(__dirname,"..","book-assets");let issues=[];function walk(d){for(const n of fs.readdirSync(d)){const p=path.join(d,n),s=fs.statSync(p);if(s.isDirectory())walk(p);else if(n.endsWith(".svg")){const t=fs.readFileSync(p,"utf8");if(!/viewBox=["']0 0 1024 1024["']/.test(t))issues.push(`${p}: brak viewBox 0 0 1024 1024`);}}}walk(root);fs.mkdirSync(path.join(__dirname,"reports"),{recursive:true});fs.writeFileSync(path.join(__dirname,"reports","asset-audit.txt"),issues.length?issues.join("\n"):"OK — brak problemów");console.log(issues.length?issues.join("\n"):"OK — brak problemów");