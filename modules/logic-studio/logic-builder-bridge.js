"use strict";
(()=>{
  const SYMBOLS=["●","▲","■","◆","★","♥","☀","✿"];
  const cache=new Map();
  const clamp=(n,a,b)=>Math.max(a,Math.min(b,n));
  const pick=(a,r)=>a[Math.floor(r()*a.length)];
  function seeded(seed){let h=2166136261;seed=String(seed||"fenix");for(let i=0;i<seed.length;i++)h=Math.imul(h^seed.charCodeAt(i),16777619);return()=>{h+=0x6D2B79F5;let t=h;t=Math.imul(t^t>>>15,t|1);t^=t+Math.imul(t^t>>>7,t|61);return((t^t>>>14)>>>0)/4294967296}}
  function shuffle(a,r){const out=a.slice();for(let i=out.length-1;i>0;i--){const j=Math.floor(r()*(i+1));[out[i],out[j]]=[out[j],out[i]]}return out}
  function makeCanvas(){const c=document.createElement("canvas");c.width=850;c.height=1100;return c}
  function base(x,o){x.fillStyle="#fff";x.fillRect(0,0,850,1100);x.strokeStyle="#111827";x.lineWidth=3;x.strokeRect(36,36,778,1028);x.fillStyle="#111827";x.textAlign="center";x.font="700 38px Arial";x.fillText(String(o.title||"Logic Challenge").slice(0,60),425,92);x.font="20px Arial";x.fillText(String(o.instructions||"Complete the activity.").slice(0,95),425,132)}
  function solutionBase(x,o){x.fillStyle="#fff";x.fillRect(0,0,850,1100);x.strokeStyle="#111827";x.lineWidth=3;x.strokeRect(36,36,778,1028);x.fillStyle="#111827";x.textAlign="center";x.font="700 34px Arial";x.fillText(`Solution — ${String(o.title||"Logic Challenge").slice(0,50)}`,425,90)}
  function circleMark(x,cx,cy,r=25){x.save();x.strokeStyle="#111";x.lineWidth=4;x.beginPath();x.arc(cx,cy,r,0,Math.PI*2);x.stroke();x.restore()}
  function drawAsset(x,img,cx,cy,size=66){if(!img)return;const iw=img.naturalWidth||img.width||1,ih=img.naturalHeight||img.height||1,fit=Math.min(size/iw,size/ih),w=iw*fit,h=ih*fit;x.drawImage(img,cx-w/2,cy-h/2,w,h)}
  function loadImage(asset){return new Promise(resolve=>{const img=new Image();img.onload=()=>resolve(img);img.onerror=()=>resolve(null);img.src=asset?.dataUrl||""})}
  function assetById(id){return FenixCore.getAsset(id)||null}
  function requirements(type){if(type==="sudoku")return{min:4,exact:true};if(type==="analogy")return{min:4};if(type==="matrix")return{min:3};if(type==="odd")return{min:2};return{min:2}}
  function selectionReady(type,n){const req=requirements(type);return req.exact?n===req.min:n>=req.min}
  function optionsFromPage(page){const s=page.recipe?.settings||{},c=page.recipe?.content||{};return{type:s.type||"sequence",title:page.title||page.recipe?.title||"Logic Challenge",instructions:s.instructions||"Complete the activity.",seed:page.recipe?.seed||page.seed||"fenix-logic-001",difficulty:s.difficulty||"medium",age:s.age||"5-7",language:s.language||"en",range:Number(s.range)||20,elementMode:s.elementMode||"symbols",assetRefs:Array.isArray(c.assetRefs)?c.assetRefs:[],oddRef:c.oddRef||null}}
  async function assetBundle(o,r){if(o.elementMode!=="assets")return null;const assets=o.assetRefs.map(assetById).filter(Boolean);if(!selectionReady(o.type,assets.length))return null;const chosen=o.type==="sudoku"?assets.slice(0,4):shuffle(assets,r),images=new Map;await Promise.all(chosen.map(async a=>images.set(a.id,await loadImage(a))));return{assets:chosen,images,odd:o.oddRef?assetById(o.oddRef):null}}
  async function renderPage(page){const o=optionsFromPage(page),r=seeded(o.seed),task=makeCanvas(),solution=makeCanvas(),p=task.getContext("2d"),s=solution.getContext("2d");base(p,o);solutionBase(s,o);p.fillStyle=s.fillStyle="#111";p.strokeStyle=s.strokeStyle="#111";p.textAlign=s.textAlign="center";const bundle=await assetBundle(o,r),useAssets=Boolean(bundle);
    if(o.type==="odd"){
      if(useAssets){const odd=bundle.odd&&o.assetRefs.includes(bundle.odd.id)?bundle.odd:bundle.assets[bundle.assets.length-1],commons=bundle.assets.filter(a=>a.id!==odd.id);for(let row=0;row<6;row++){const n=o.difficulty==="hard"?7:o.difficulty==="easy"?4:5,common=pick(commons,r),pos=Math.floor(r()*n);for(let i=0;i<n;i++){const x=145+i*105,y=245+row*120,a=i===pos?odd:common,img=bundle.images.get(a.id);drawAsset(p,img,x,y-16,66);drawAsset(s,img,x,y-16,66);if(i===pos)circleMark(s,x,y-16,42)}}}
      else{for(let row=0;row<6;row++){const n=o.difficulty==="hard"?7:o.difficulty==="easy"?4:5,common=pick(SYMBOLS,r),odd=pick(SYMBOLS.filter(x=>x!==common),r),pos=Math.floor(r()*n);p.font=s.font="46px Arial";for(let i=0;i<n;i++){const x=145+i*105,y=245+row*120,value=i===pos?odd:common;p.fillText(value,x,y);s.fillText(value,x,y);if(i===pos)circleMark(s,x,y-15,34)}}}
    }else if(o.type==="sudoku"){
      const grid=[[0,1,2,3],[2,3,0,1],[1,0,3,2],[3,2,1,0]],blank=new Set(["0,0","1,2","2,1","3,3"]),arr=useAssets?bundle.assets.slice(0,4):SYMBOLS.slice(0,4),gridTop=useAssets?330:270;
      if(useAssets){for(const x of[p,s]){x.fillStyle="#111827";x.textAlign="center";x.font="700 17px Arial";x.fillText("PICTURE SET",425,178)}arr.forEach((a,i)=>{const img=bundle.images.get(a.id),cx=290+i*90;drawAsset(p,img,cx,225,58);drawAsset(s,img,cx,225,58)});p.strokeStyle=s.strokeStyle="#c5ccd3";p.lineWidth=s.lineWidth=1;for(let i=0;i<4;i++){p.strokeRect(255+i*90,188,70,74);s.strokeRect(255+i*90,188,70,74)}}
      for(let y=0;y<4;y++)for(let x=0;x<4;x++){const px=215+x*105,py=gridTop+y*105,val=arr[grid[y][x]];p.strokeStyle=s.strokeStyle="#111";p.lineWidth=s.lineWidth=3;p.strokeRect(px,py,105,105);s.strokeRect(px,py,105,105);if(useAssets){const img=bundle.images.get(val.id);if(!blank.has(`${y},${x}`))drawAsset(p,img,px+52,py+52,70);drawAsset(s,img,px+52,py+52,70)}else{p.font=s.font="46px Arial";if(!blank.has(`${y},${x}`))p.fillText(val,px+52,py+68);s.fillText(val,px+52,py+68)}}
    }else if(o.type==="matrix"){
      const arr=useAssets?bundle.assets.slice(0,3):shuffle(SYMBOLS,r).slice(0,3);for(let y=0;y<3;y++)for(let x=0;x<3;x++){const px=245+x*120,py=280+y*120,val=arr[(x+y)%3],missing=x===2&&y===2;p.strokeRect(px,py,120,120);s.strokeRect(px,py,120,120);if(useAssets){const img=bundle.images.get(val.id);if(!missing)drawAsset(p,img,px+60,py+60,78);else{p.font="50px Arial";p.fillText("?",px+60,py+78)}drawAsset(s,img,px+60,py+60,78)}else{p.font=s.font="48px Arial";p.fillText(missing?"?":val,px+60,py+75);s.fillText(val,px+60,py+75)}}
    }else if(o.type==="analogy"){
      if(useAssets){const arr=bundle.assets.slice(0,4),correct=arr[3],extras=bundle.assets.slice(4),choices=shuffle([correct,...extras.slice(0,2)],r);while(choices.length<3)choices.push(arr[choices.length%3]);const positions=[[315,300],[535,300],[315,455]];positions.forEach((pos,i)=>{drawAsset(p,bundle.images.get(arr[i].id),pos[0],pos[1],90);drawAsset(s,bundle.images.get(arr[i].id),pos[0],pos[1],90)});p.font=s.font="48px Arial";p.fillText("→",425,315);s.fillText("→",425,315);p.fillText("→",425,470);s.fillText("→",425,470);p.fillText("?",535,470);drawAsset(s,bundle.images.get(correct.id),535,455,90);choices.slice(0,3).forEach((a,i)=>{const x=280+i*145;drawAsset(p,bundle.images.get(a.id),x,680,82);drawAsset(s,bundle.images.get(a.id),x,680,82);if(a.id===correct.id)circleMark(s,x,680,52)})}
      else{const values=shuffle(SYMBOLS,r).slice(0,4),correct=values[3],choices=shuffle([correct,...SYMBOLS.filter(x=>!values.includes(x)).slice(0,2)],r);p.font=s.font="58px Arial";p.fillText(`${values[0]}  →  ${values[1]}`,425,310);p.fillText(`${values[2]}  →  ?`,425,440);s.fillText(`${values[0]}  →  ${values[1]}`,425,310);s.fillText(`${values[2]}  →  ${correct}`,425,440);choices.forEach((value,i)=>{const x=280+i*145;p.fillText(value,x,650);s.fillText(value,x,650);if(value===correct)circleMark(s,x,630,38)})}
    }else{
      if(useAssets){const pattern=bundle.assets.slice(0,Math.min(3,bundle.assets.length));for(let row=0;row<6;row++){const n=o.difficulty==="hard"?9:7;for(let i=0;i<n;i++){const x=95+i*90,y=245+row*115,a=pattern[i%pattern.length],img=bundle.images.get(a.id);if(i===n-1){p.font="48px Arial";p.fillText("?",x,y)}else drawAsset(p,img,x,y-18,62);drawAsset(s,img,x,y-18,62)}}}
      else{for(let row=0;row<6;row++){const pattern=shuffle(SYMBOLS,r).slice(0,3),n=o.difficulty==="hard"?9:7;p.font=s.font="48px Arial";for(let i=0;i<n;i++){const x=95+i*90,y=245+row*115,val=pattern[i%3];p.fillText(i===n-1?"?":val,x,y);s.fillText(val,x,y)}}}
    }
    return{pageCanvas:task,solutionCanvas:solution};
  }
  function scaled(canvas,scale){if(scale===1)return canvas;const out=document.createElement("canvas");out.width=canvas.width*scale;out.height=canvas.height*scale;out.getContext("2d").drawImage(canvas,0,0,out.width,out.height);return out}
  async function prepare(){const logicPages=FenixCore.getCart().filter(page=>FenixPageSchema.moduleOf(page)==="logic-studio");for(const page of logicPages){const key=String(page.recipe?.seed||page.seed||page.id);try{const rendered=await renderPage(page);cache.set(key,rendered);const normalized=FenixPageSchema.normalize(page);normalized.preview={...(normalized.preview||{}),imageData:rendered.pageCanvas.toDataURL("image/png")};normalized.solution={...(normalized.solution||{}),available:true,imageData:rendered.solutionCanvas.toDataURL("image/png")};FenixCore.updatePage(page.id,normalized)}catch(error){console.error("Logic migration failed",page?.id,error)}}}
  async function boot(){await prepare();const original=window.FenixStandardRenderers;if(original){window.FenixStandardRenderers={...original,render(module,o,seed,pageNo,scale=1){if(module==="logic-studio"){const found=cache.get(String(seed));if(found)return{pageCanvas:scaled(found.pageCanvas,scale),solutionCanvas:scaled(found.solutionCanvas,scale),hasSolution:true};}return original.render(module,o,seed,pageNo,scale)}}const script=document.createElement("script");script.src="book-builder.js?v=0.28.4";script.defer=false;document.body.appendChild(script)}
  boot().catch(error=>{console.error("Logic bridge boot failed",error);const script=document.createElement("script");script.src="book-builder.js?v=0.28.4";document.body.appendChild(script)});
})();
