"use strict";
(()=>{
 const $=id=>document.getElementById(id);
 const core=typeof FenixCore!=="undefined"?FenixCore:null;
 const schema=typeof FenixPageSchema!=="undefined"?FenixPageSchema:null;
 if(!core||!schema)return;
 let saving=false;
 const fieldIds=["title","subtitle","instruction","titleSize","titleY","subtitleSize","instructionSize","ageProfile","difficulty","cols","rows","lineWidth","endpointMode","wallStyle","seed","startAsset","goalAsset","checkpointAsset","hazardAsset","startAssetScale","goalAssetScale","checkpointCount","checkpointScale","hazardCount","hazardScale","decoCount","decoScale","sideMargin","topMargin","bottomMargin","mazeScale","solution"];
 const num=(id,fallback)=>{const v=Number($(id)?.value);return Number.isFinite(v)?v:fallback};
 const selectedDeco=()=>[...document.querySelectorAll('#decoAssetChoices input[type="checkbox"]:checked')].map(x=>x.value);
 function currentPage(){const id=$("pageSelect")?.value;return id?(core.getCart()||[]).find(p=>p.id===id&&p.module==="maze-studio")||null:null}
 function settings(){const existing=currentPage()?.recipe?.settings||{},difficulty=window.FenixMazeDifficulty?.values?.()||{};return{ageProfile:$("ageProfile")?.value||"4-6",difficulty:$("difficulty")?.value||"custom",cols:Math.max(6,Math.min(45,num("cols",18))),rows:Math.max(8,Math.min(58,num("rows",24))),lineWidth:Math.max(2,Math.min(12,num("lineWidth",5))),...difficulty,typographyVersion:2,titleSize:Math.max(72,Math.min(160,num("titleSize",112))),titleY:Math.max(140,Math.min(420,num("titleY",230))),subtitle:$("subtitle")?.value||"",instruction:$("instruction")?.value||"",subtitleSize:Math.max(30,Math.min(90,num("subtitleSize",52))),instructionSize:Math.max(30,Math.min(90,num("instructionSize",44))),sideMargin:Math.max(35,Math.min(150,num("sideMargin",80))),topMargin:Math.max(120,Math.min(300,num("topMargin",170))),bottomMargin:Math.max(55,Math.min(180,num("bottomMargin",90))),mazeScale:Math.max(55,Math.min(100,num("mazeScale",100))),endpointMode:$("endpointMode")?.value||"random",wallStyle:$("wallStyle")?.value||"clean",showSolution:$("solution")?.value==="yes",startAssetRef:$("startAsset")?.value||null,goalAssetRef:$("goalAsset")?.value||null,checkpointAssetRef:$("checkpointAsset")?.value||null,hazardAssetRef:$("hazardAsset")?.value||null,startAssetScale:Math.max(40,Math.min(180,num("startAssetScale",100))),goalAssetScale:Math.max(40,Math.min(180,num("goalAssetScale",100))),checkpointCount:Math.max(0,Math.min(5,num("checkpointCount",0))),checkpointScale:Math.max(40,Math.min(160,num("checkpointScale",80))),hazardCount:Math.max(0,Math.min(8,num("hazardCount",0))),hazardScale:Math.max(40,Math.min(160,num("hazardScale",78))),decoAssetRefs:selectedDeco(),decoCount:Math.max(0,Math.min(10,num("decoCount",0))),decoScale:Math.max(30,Math.min(160,num("decoScale",80))),endpoints:existing.endpoints||null,showPageNumber:false,pageNumber:1}}
 function canvasSnapshot(){const canvas=$("page");try{return canvas&&canvas.width&&canvas.height?canvas.toDataURL("image/png"):null}catch{return null}}
 function payload(){const existing=currentPage(),s=settings(),seed=Number($("seed")?.value)||1,title=$("title")?.value?.trim()||"Find the Way!",shot=canvasSnapshot(),activityShot=s.showSolution?(existing?.preview?.imageData||null):shot,solutionShot=s.showSolution?shot:(existing?.solution?.imageData||null);return schema.normalize({module:"maze-studio",title,seed,settings:s,recipe:{module:"maze-studio",seed,title,settings:s,meta:{createdWith:"FENIX PC",desktopEditedAt:new Date().toISOString(),renderState:{showSolution:s.showSolution,endpoints:s.endpoints}}},preview:{imageData:activityShot},solution:{available:true,imageData:solutionShot},source:{app:"fenix-desktop",version:"0.34.0",format:"native"}})}
 function syncDifficulty(){const page=currentPage();if(page)window.FenixMazeDifficulty?.apply?.(page.recipe?.settings||page.settings||null)}
 function snapshot(){return{values:Object.fromEntries(fieldIds.map(id=>[id,$(id)?.value??null])),deco:new Set(selectedDeco())}}
 function restore(snap){for(const[id,value]of Object.entries(snap.values)){const el=$(id);if(el&&value!==null)el.value=value}document.querySelectorAll('#decoAssetChoices input[type="checkbox"]').forEach(input=>input.checked=snap.deco.has(input.value))}
 function ensureSelectEntry(savedId){const select=$("pageSelect");if(!select||!savedId)return;const page=(core.getCart()||[]).find(p=>p.id===savedId);if(!page)return;let option=[...select.options].find(o=>o.value===savedId);if(!option){option=document.createElement("option");option.value=savedId;select.appendChild(option)}option.textContent=page.title||"Labirynt";select.value=savedId}
 function save(copy){const page=currentPage(),data=payload(),snap=snapshot();saving=true;let savedId=null;try{if(page&&!copy){core.updatePage(page.id,data);savedId=page.id}else{core.addPage(data);savedId=core.getCart().at(-1)?.id||null}}finally{restore(snap);ensureSelectEntry(savedId);saving=false}const trigger=$("startAssetScale");if(trigger)trigger.dispatchEvent(new Event("input",{bubbles:true}));return savedId}
 window.addEventListener("fenix-state-change",event=>{if(saving)return;if(event.detail?.activeProject)setTimeout(syncDifficulty,0)},true);
 document.addEventListener("click",event=>{const id=event.target?.id;if(id!=="saveCart"&&id!=="saveCopy")return;event.preventDefault();event.stopImmediatePropagation();const savedId=save(id==="saveCopy");const status=$("status");if(status)status.textContent=id==="saveCopy"?"Kopia dodana do Stron projektu — zapisano dokładny obraz strony.":"Strona zapisana w Stronach projektu — zapisano dokładny obraz strony.";ensureSelectEntry(savedId)},true);
 $("pageSelect")?.addEventListener("change",()=>setTimeout(syncDifficulty,0));
 setTimeout(syncDifficulty,0);
 window.FenixMazeSave=Object.freeze({payload,settings});
})();
