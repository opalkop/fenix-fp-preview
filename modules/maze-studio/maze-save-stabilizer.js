"use strict";
(()=>{
 const $=id=>document.getElementById(id);
 const core=typeof FenixCore!=="undefined"?FenixCore:null;
 const schema=typeof FenixPageSchema!=="undefined"?FenixPageSchema:null;
 if(!core||!schema)return;
 const num=(id,fallback)=>{const v=Number($(id)?.value);return Number.isFinite(v)?v:fallback};
 const selectedDeco=()=>[...document.querySelectorAll('#decoAssetChoices input[type="checkbox"]:checked')].map(x=>x.value);
 function currentPage(){const id=$("pageSelect")?.value;return id?(core.getCart()||[]).find(p=>p.id===id&&p.module==="maze-studio")||null:null}
 function settings(){const existing=currentPage()?.recipe?.settings||{},difficulty=window.FenixMazeDifficulty?.values?.()||{};return{ageProfile:$("ageProfile")?.value||"4-6",difficulty:$("difficulty")?.value||"custom",cols:Math.max(6,Math.min(45,num("cols",18))),rows:Math.max(8,Math.min(58,num("rows",24))),lineWidth:Math.max(2,Math.min(12,num("lineWidth",5))),...difficulty,typographyVersion:2,titleSize:Math.max(72,Math.min(160,num("titleSize",112))),titleY:Math.max(140,Math.min(420,num("titleY",230))),subtitle:$("subtitle")?.value||"",instruction:$("instruction")?.value||"",subtitleSize:Math.max(30,Math.min(90,num("subtitleSize",52))),instructionSize:Math.max(30,Math.min(90,num("instructionSize",44))),sideMargin:Math.max(35,Math.min(150,num("sideMargin",80))),topMargin:Math.max(120,Math.min(300,num("topMargin",170))),bottomMargin:Math.max(55,Math.min(180,num("bottomMargin",90))),mazeScale:Math.max(55,Math.min(100,num("mazeScale",100))),endpointMode:$("endpointMode")?.value||"random",wallStyle:$("wallStyle")?.value||"clean",showSolution:$("solution")?.value==="yes",startAssetRef:$("startAsset")?.value||null,goalAssetRef:$("goalAsset")?.value||null,checkpointAssetRef:$("checkpointAsset")?.value||null,hazardAssetRef:$("hazardAsset")?.value||null,startAssetScale:Math.max(40,Math.min(180,num("startAssetScale",100))),goalAssetScale:Math.max(40,Math.min(180,num("goalAssetScale",100))),checkpointCount:Math.max(0,Math.min(5,num("checkpointCount",0))),checkpointScale:Math.max(40,Math.min(160,num("checkpointScale",80))),hazardCount:Math.max(0,Math.min(8,num("hazardCount",0))),hazardScale:Math.max(40,Math.min(160,num("hazardScale",78))),decoAssetRefs:selectedDeco(),decoCount:Math.max(0,Math.min(10,num("decoCount",0))),decoScale:Math.max(30,Math.min(160,num("decoScale",80))),endpoints:existing.endpoints||null,showPageNumber:false,pageNumber:1}}
 function payload(){const s=settings(),seed=Number($("seed")?.value)||1,title=$("title")?.value?.trim()||"Find the Way!";return schema.normalize({module:"maze-studio",title,seed,settings:s,recipe:{module:"maze-studio",seed,title,settings:s,meta:{createdWith:"FENIX PC",desktopEditedAt:new Date().toISOString(),renderState:{showSolution:s.showSolution,endpoints:s.endpoints}}},solution:{available:true,imageData:null},source:{app:"fenix-desktop",version:"0.31.0",format:"native"}})}
 function syncDifficulty(){const page=currentPage();if(page)window.FenixMazeDifficulty?.apply?.(page.recipe?.settings||page.settings||null)}
 function save(copy){const page=currentPage(),data=payload();if(page&&!copy){core.updatePage(page.id,data);return page.id}core.addPage(data);return core.getCart().at(-1)?.id||null}
 document.addEventListener("click",event=>{const id=event.target?.id;if(id!=="saveCart"&&id!=="saveCopy")return;event.preventDefault();event.stopImmediatePropagation();const savedId=save(id==="saveCopy");const status=$("status");if(status)status.textContent=id==="saveCopy"?"Kopia dodana do Stron projektu.":"Strona zapisana w Stronach projektu.";setTimeout(()=>{const select=$("pageSelect");if(select&&savedId){select.value=savedId;select.dispatchEvent(new Event("change",{bubbles:true}))}},0)},true);
 $("pageSelect")?.addEventListener("change",()=>setTimeout(syncDifficulty,0));
 window.addEventListener("fenix-state-change",event=>{if(event.detail?.activeProject)setTimeout(syncDifficulty,0)});
 setTimeout(syncDifficulty,0);
 window.FenixMazeSave=Object.freeze({payload,settings});
})();
