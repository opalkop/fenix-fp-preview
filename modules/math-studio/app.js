"use strict";
(async()=>{await FenixCore.ready;const TYPE="math-studio",$=id=>document.getElementById(id),canvas=$("page"),ctx=canvas.getContext("2d"),editId=new URLSearchParams(location.search).get("id");canvas.width=850;canvas.height=1100;const val=id=>$(id)?.value||"";function seeded(seed){let x=0;for(const c of String(seed))x=(x*31+c.charCodeAt(0))>>>0;return()=>((x=(1664525*x+1013904223)>>>0)/4294967296)}function settings(){return{type:val("type"),preset:val("preset"),difficulty:val("difficulty"),age:val("age"),count:Number(val("count"))||12,range:Number(val("range"))||20,title:val("title"),instructions:val("instructions"),seed:val("seed"),language:val("language")}}function draw(){const o=settings(),r=seeded(o.seed);ctx.clearRect(0,0,850,1100);ctx.fillStyle="#fff";ctx.fillRect(0,0,850,1100);ctx.strokeStyle="#111827";ctx.lineWidth=3;ctx.strokeRect(40,40,770,1020);ctx.fillStyle="#111827";ctx.textAlign="center";ctx.font="700 40px Arial";ctx.fillText(o.title||"Math Practice",425,105);ctx.font="20px Arial";ctx.fillText(o.instructions||"Solve the addition problems.",425,145);ctx.textAlign="left";ctx.font="700 34px Arial";const n=Math.max(6,Math.min(12,o.count)),max=Math.max(5,o.range);for(let i=0;i<n;i++){let a=1+Math.floor(r()*Math.max(2,max/2)),b=1+Math.floor(r()*Math.max(2,max/2));if(o.difficulty==="easy"){a=Math.min(a,10);b=Math.min(b,10)}const x=115+(i%2)*385,y=250+Math.floor(i/2)*120;ctx.fillText(`${a} + ${b} = ______`,x,y)}}function payload(){const o=settings();return FenixPageSchema.normalize({module:TYPE,title:o.title||"Math Practice",seed:o.seed,recipe:{module:TYPE,seed:o.seed,title:o.title,settings:o,content:{},meta:{moduleVersion:2,renderer:"math-v2",lightweight:true}},preview:{imageData:null},solution:{available:true,imageData:null},source:{app:"fenix-portable-preview",version:"2.1",format:"native"}})}function pages(){return FenixCore.getCart().filter(p=>(p.module||p.recipe?.module)===TYPE)}function renderList(){const list=pages();$("savedCount").textContent=`${list.length} ${list.length===1?"strona":"stron"}`;$("savedList").innerHTML=list.length?list.map((p,i)=>{const s=p.recipe?.settings||p.settings||{};return `<article class="saved-item${String(p.id)===String(editId)?' current':''}"><div class="thumb">+</div><div><strong>Math #${String(i+1).padStart(2,'0')} · ${p.title||'Math Practice'}</strong><div class="hint">${s.difficulty||'medium'} · ${s.age||'5-7'} · zakres ${s.range||20}</div></div><div class="item-actions"><button data-edit="${p.id}">Edytuj</button><button data-del="${p.id}">Usuń</button></div></article>`}).join(""):'<div class="hint">Nie zapisano jeszcze stron Math Studio.</div>';document.querySelectorAll("[data-edit]").forEach(b=>b.onclick=()=>location.href=`${location.pathname}?id=${encodeURIComponent(b.dataset.edit)}`);document.querySelectorAll("[data-del]").forEach(b=>b.onclick=()=>{if(confirm("Usunąć tę stronę Math?")){FenixCore.removePage(b.dataset.del);renderList()}})}function restore(){if(!editId)return;const p=pages().find(x=>String(x.id)===String(editId));if(!p)return;const s=p.recipe?.settings||p.settings||{};Object.entries(s).forEach(([k,v])=>{if($(k)&&v!=null)$(k).value=v});$("cart").textContent="Zapisz zmiany w projekcie";$("editBadge").textContent="TRYB EDYCJI · zapisujesz tę samą stronę";draw()}function save(){try{const data=payload();if(editId){FenixCore.updatePage(editId,data);$("status").textContent="✓ Zapisano zmiany strony Math."}else{FenixCore.addPage(data);$("status").textContent="✓ Dodano stronę Math do projektu."}renderList()}catch(e){$("status").textContent=`Błąd zapisu: ${e?.message||e}`}}$("generate").onclick=draw;$("randomSeed").onclick=()=>{$("seed").value=`fenix-math-${Date.now().toString(36)}`;draw()};$("png").onclick=()=>FenixCore.downloadCanvas(canvas,`math-${Date.now()}.png`);$("newPage").onclick=()=>location.href=location.pathname;$("cart").onclick=save;["type","preset","difficulty","age","count","range","title","instructions","seed","language"].forEach(id=>$(id)?.addEventListener("input",draw));const p=FenixCore.getActiveProject();$("projectInfo").textContent=`Aktywny projekt: ${p.name} · ${p.format}`;restore();draw();renderList();window.addEventListener("fenix-state-change",renderList)})();