"use strict";
window.FenixSync=(()=>{
  const CONFIG_KEY="fenix-sync-config-v1",TOKEN_KEY="fenix-sync-token-v1",FORMAT="FENIX_SYNC_BUNDLE",VERSION=2,DEFAULTS=Object.freeze({provider:"github",owner:"opalkop",repo:"fenix",branch:"fenix-sync-data",path:"fenix-sync/projects.json",autoSync:true});
  const safeParse=(v,fallback)=>{try{return JSON.parse(v)||fallback}catch{return fallback}},readConfig=()=>safeParse(localStorage.getItem(CONFIG_KEY),{});
  let syncPromise=null;
  function getConfig(){const c=readConfig();return{...DEFAULTS,...c,token:sessionStorage.getItem(TOKEN_KEY)||c.token||"",rememberToken:Boolean(c.token),autoSync:c.autoSync!==false}}
  function setConfig(next={}){const merged={...getConfig(),...next},persistent={provider:"github",owner:String(merged.owner||DEFAULTS.owner).trim(),repo:String(merged.repo||DEFAULTS.repo).trim(),branch:String(merged.branch||DEFAULTS.branch).trim(),path:String(merged.path||DEFAULTS.path).trim(),autoSync:merged.autoSync!==false};if(next.rememberToken&&merged.token)persistent.token=merged.token;localStorage.setItem(CONFIG_KEY,JSON.stringify(persistent));if(merged.token)sessionStorage.setItem(TOKEN_KEY,merged.token);else sessionStorage.removeItem(TOKEN_KEY);return getConfig()}
  function clearToken(){sessionStorage.removeItem(TOKEN_KEY);const c=readConfig();delete c.token;localStorage.setItem(CONFIG_KEY,JSON.stringify(c))}
  function snapshot(){return{type:FORMAT,version:VERSION,schemaVersion:window.FenixPageSchema?.VERSION||3,updatedAt:new Date().toISOString(),activeId:FenixCore.getActiveProjectId(),projects:FenixCore.getProjects()}}
  const newer=(a,b)=>String(a?.updatedAt||a?.createdAt||"").localeCompare(String(b?.updatedAt||b?.createdAt||""))>0;
  function mergeBundle(bundle,{prefer="newer"}={}){if(!bundle||bundle.type!==FORMAT||!Array.isArray(bundle.projects))throw new Error("Nieprawidłowy pakiet synchronizacji FENIX.");const byId=new Map(FenixCore.getProjects().map(p=>[p.id,p])),stats={created:0,updated:0,kept:0};for(const remote of bundle.projects){const here=byId.get(remote.id);if(!here){FenixCore.createProject(remote);stats.created++;byId.set(remote.id,remote);continue}const take=prefer==="remote"||prefer==="newer"&&newer(remote,here);if(take){FenixCore.updateProject(here.id,remote);stats.updated++}else stats.kept++}if(bundle.activeId&&FenixCore.getProjects().some(p=>p.id===bundle.activeId))FenixCore.setActiveProject(bundle.activeId);return stats}
  function b64utf8(text){const bytes=new TextEncoder().encode(text);let bin="";bytes.forEach(b=>bin+=String.fromCharCode(b));return btoa(bin)}
  function unb64utf8(text){const bin=atob(String(text||"").replace(/\n/g,"")),bytes=Uint8Array.from(bin,c=>c.charCodeAt(0));return new TextDecoder().decode(bytes)}
  function requireConfig(){const c=getConfig();if(!c.token)throw new Error("Fenix Sync wymaga jednorazowego tokenu GitHub na tym urządzeniu.");return c}
  async function request(url,options={},allowed=[404]){const c=requireConfig(),headers={Accept:"application/vnd.github+json","X-GitHub-Api-Version":"2022-11-28",Authorization:`Bearer ${c.token}`,...options.headers},response=await fetch(url,{...options,headers});if(!response.ok&&!allowed.includes(response.status)){const body=await response.text().catch(()=>"");throw new Error(`GitHub Sync ${response.status}: ${body.slice(0,220)}`)}return response}
  const apiPath=c=>`${c.path.split('/').map(encodeURIComponent).join('/')}`;
  const contentsUrl=c=>`https://api.github.com/repos/${encodeURIComponent(c.owner)}/${encodeURIComponent(c.repo)}/contents/${apiPath(c)}?ref=${encodeURIComponent(c.branch)}`;
  const writeUrl=c=>`https://api.github.com/repos/${encodeURIComponent(c.owner)}/${encodeURIComponent(c.repo)}/contents/${apiPath(c)}`;
  async function readMeta(c){const res=await request(contentsUrl(c));if(res.status===404)return null;return res.json()}
  async function decodeMetaContent(meta){if(meta?.content){const text=unb64utf8(meta.content);if(text.trim())return text}if(meta?.git_url){const res=await request(meta.git_url,{},[]),blob=await res.json();if(blob?.content){const text=unb64utf8(blob.content);if(text.trim())return text}}throw new Error("GitHub zwrócił pustą zawartość projects.json. Odśwież stronę i spróbuj ponownie.")}
  async function pull({merge=true}={}){const c=requireConfig(),meta=await readMeta(c);if(!meta)return{found:false,bundle:null,stats:null};const text=await decodeMetaContent(meta);let bundle;try{bundle=JSON.parse(text)}catch{throw new Error("projects.json w Fenix Sync jest uszkodzony lub niekompletny.")}const stats=merge?mergeBundle(bundle):null;localStorage.setItem("fenix-sync-last-pull",new Date().toISOString());return{found:true,bundle,stats,sha:meta.sha}}
  async function pushOnce(c,bundle){const meta=await readMeta(c),body={message:`Fenix Sync ${new Date().toISOString()}`,content:b64utf8(JSON.stringify(bundle)),branch:c.branch};if(meta?.sha)body.sha=meta.sha;const res=await request(writeUrl(c),{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)},[409,404]);if(res.status===409)return{conflict:true};if(res.status===404)throw new Error("Fenix Sync nie znalazł gałęzi lub repozytorium danych.");const data=await res.json();return{conflict:false,commit:data.commit?.sha||null}}
  async function push(){const c=requireConfig(),bundle=snapshot();let result=await pushOnce(c,bundle);if(result.conflict){await new Promise(resolve=>setTimeout(resolve,350));result=await pushOnce(c,snapshot());if(result.conflict)throw new Error("Plik synchronizacji zmienił się równocześnie na innym urządzeniu. Kliknij „Pobierz”, a potem „Wyślij”.")}localStorage.setItem("fenix-sync-last-push",new Date().toISOString());return{bundle,commit:result.commit}}
  async function doSync(){const remote=await pull({merge:true}),pushed=await push();window.dispatchEvent(new CustomEvent("fenix-sync-complete",{detail:{remote,pushed}}));return{remote,pushed}}
  function sync(){if(syncPromise)return syncPromise;syncPromise=doSync().finally(()=>{syncPromise=null});return syncPromise}
  async function auto(){const c=getConfig();if(!c.autoSync||!c.token)return{skipped:true};try{return await sync()}catch(error){console.warn("Fenix auto sync",error);return{error:String(error.message||error)}}}
  return Object.freeze({FORMAT,VERSION,DEFAULTS,getConfig,setConfig,clearToken,snapshot,mergeBundle,pull,push,sync,auto});
})();